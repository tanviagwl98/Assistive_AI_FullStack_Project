import { model, models, Schema } from "mongoose";
const schema = new Schema({ weddingId: { type: Schema.Types.ObjectId, required: true, ref: "Wedding" }, email: { type: String, required: true }, emailNormalized: { type: String, required: true, index: true }, role: { type: String, enum: ["ADMIN", "MANAGER"], required: true }, tokenHash: { type: String, required: true, unique: true }, status: { type: String, enum: ["PENDING", "ACCEPTED", "REVOKED"], default: "PENDING" }, invitedByUserId: { type: Schema.Types.ObjectId, required: true, ref: "User" }, acceptedByUserId: { type: Schema.Types.ObjectId, ref: "User" }, expiresAt: { type: Date, required: true, index: true }, acceptedAt: Date }, { timestamps: true });
schema.index({ weddingId: 1, status: 1 }); schema.index({ weddingId: 1, emailNormalized: 1 }, { unique: true, partialFilterExpression: { status: "PENDING" } });
export const WeddingMemberInvitation = models.WeddingMemberInvitation ?? model("WeddingMemberInvitation", schema);
