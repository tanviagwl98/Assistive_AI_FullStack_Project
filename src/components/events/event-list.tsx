import Link from "next/link";
import { formatDateTime } from "@/components/workspace/format";
export function EventList({ events, timeZone, includeArchived }: any) { return <main className="container py-10"><div className="flex items-end justify-between gap-4"><div><p className="eyebrow">Wedding planning</p><h1 className="mt-2">Events</h1></div><Link href="/events/new" className="button button-primary">Add event</Link></div><div className="mt-7 grid gap-4">{events?.map((event: any) => <Link key={event.id} href={`/events/${event.id}`} className="rounded-card border bg-surface p-5 shadow-soft"><div className="flex flex-wrap justify-between gap-2"><div><h2 className="text-2xl">{event.name}</h2><p className="mt-1 text-muted-foreground">{formatDateTime(event.startsAt, timeZone)} · {event.venueName || "Venue to be confirmed"}</p></div>{event.archivedAt && <span className="rounded-full bg-surface-muted px-3 py-1 text-xs">Archived</span>}</div></Link>)}{!events?.length && <section className="rounded-card border border-dashed p-8 text-center"><h2>No events yet</h2><p className="mt-2 text-muted-foreground">Add ceremonies such as Mehendi, Haldi, Sangeet, Wedding and Reception.</p></section>}</div>{includeArchived && <p className="mt-5 text-sm text-muted-foreground">Showing archived events too.</p>}</main>; }
