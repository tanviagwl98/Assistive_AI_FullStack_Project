/* eslint-disable @typescript-eslint/no-explicit-any */
import Link from "next/link";
import { formatDate } from "@/components/workspace/format";
export function OverviewPage({ rsvp, guests, tasks, events, members, role, wedding, updated }: any) {
  const date = wedding?.weddingDate ? formatDate(wedding.weddingDate, wedding.timeZone) : "Wedding date not set";
  const cards = [["Events", Array.isArray(events) ? events.length : events?.total ?? 0, "/events"], ["Tasks", tasks?.completed !== undefined ? `${tasks.completed} / ${tasks.total}` : tasks?.total ?? 0, "/tasks"], ["Guests", guests?.total ?? 0, "/guests"], ["RSVPs", rsvp?.responded ?? rsvp?.total ?? 0, "/guests"]];
  return <main className="container py-10"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Your wedding</p><h1 className="mt-2 text-4xl">{wedding?.title || `${wedding?.brideName ?? ""} & ${wedding?.groomName ?? ""}`}</h1><p className="mt-2 text-muted-foreground">{date}</p></div><Link className="button button-secondary" href="/wedding/edit">Wedding details</Link></div>{updated && <p role="status" className="mt-6 rounded-md bg-green-50 p-3 text-green-800">Wedding details updated.</p>}<section className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([label, value, href]) => <Link href={href as string} key={label as string} className="rounded-card border bg-surface p-5 shadow-soft"><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 text-3xl font-semibold">{value as string}</p></Link>)}</section><section className="mt-10 grid gap-6 lg:grid-cols-2"><article className="rounded-card border bg-surface p-6"><h2 className="text-2xl">Upcoming events</h2><div className="mt-4 grid gap-3">{Array.isArray(events) && events.filter((event: any) => !event.archivedAt).slice(0, 4).map((event: any) => <Link key={event.id} className="rounded-md border p-3 hover:bg-surface-muted" href={`/events/${event.id}`}><strong>{event.name}</strong><span className="ml-2 text-sm text-muted-foreground">{formatDate(event.startsAt, wedding?.timeZone)}</span></Link>)}{(!Array.isArray(events) || !events.length) && <p className="text-muted-foreground">Add your ceremonies to start planning.</p>}</div></article><article className="rounded-card border bg-surface p-6"><h2 className="text-2xl">Wedding team</h2><p className="mt-3 text-muted-foreground">{members?.length ?? 0} member{members?.length === 1 ? "" : "s"} helping plan this celebration.</p>{role === "ADMIN" && <Link className="button button-secondary mt-5" href="/settings/members">Manage members</Link>}</article></section></main>;
}
