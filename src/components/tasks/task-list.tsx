import Link from "next/link";
export function TaskList({ result, choices, query, deleted }: any) { const tasks = result?.data ?? result?.tasks ?? result ?? []; return <main className="container py-10"><div className="flex items-end justify-between gap-4"><div><p className="eyebrow">Wedding planning</p><h1 className="mt-2">Tasks</h1></div><Link className="button button-primary" href="/tasks/new">Add task</Link></div>{deleted && <p className="mt-5 rounded-md bg-green-50 p-3 text-green-800">Task deleted.</p>}<form className="mt-7 flex flex-wrap gap-3 rounded-card border bg-surface p-4" method="get"><select name="status" defaultValue={query?.status ?? ""} className="rounded-md border p-2"><option value="">All statuses</option><option value="TODO">To do</option><option value="IN_PROGRESS">In progress</option><option value="COMPLETED">Completed</option></select><select name="priority" defaultValue={query?.priority ?? ""} className="rounded-md border p-2"><option value="">All priorities</option><option value="HIGH">High</option><option value="MEDIUM">Medium</option><option value="LOW">Low</option></select><button className="button button-secondary">Filter</button></form><div className="mt-5 grid gap-3">{tasks.map((task: any) => <Link key={task.id} href={`/tasks/${task.id}/edit`} className="rounded-card border bg-surface p-4 shadow-soft"><div className="flex justify-between gap-3"><div><strong>{task.title}</strong><p className="text-sm text-muted-foreground">{task.assignedMember?.name || "Unassigned"}{task.event?.name ? ` · ${task.event.name}` : ""}</p></div><span className="text-sm text-muted-foreground">{task.status?.replace("_", " ")}</span></div></Link>)}{!tasks.length && <p className="rounded-card border border-dashed p-8 text-center text-muted-foreground">No tasks match these filters.</p>}</div></main>; }
